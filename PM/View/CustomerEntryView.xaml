<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
    xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking"
    xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
    xmlns:local="clr-namespace:PM.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
    x:Class="PM.View.CustomerEntryView"
    mc:Ignorable="d"
    d:DesignHeight="450"
    d:DesignWidth="800"
    >

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Skin/MainSkin.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <UserControl.DataContext>
        <Binding Path="CustomerEntryViewModel" Source="{StaticResource Locator}"/>
    </UserControl.DataContext>

    <Grid>
        <dxdo:DockLayoutManager Margin="12">
            <dxdo:LayoutGroup Orientation="Horizontal">
                <dxdo:LayoutPanel
                    AllowSizing="True"
                    Caption="Selection Pane"
                    ItemWidth="450">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition Height="50" />
                        </Grid.RowDefinitions>
                        
                        <dxlc:GroupBox Header="Personal Clients">
                            <dxg:GridControl
                                ItemsSource="{Binding Customers}"
                                SelectedItem="{Binding SelectedCustomer}"
                                FilterString="[CustomerType] = 'Personal'">
                                <dxg:GridControl.View>
                                    <dxg:TableView AllowPerPixelScrolling="True"/>
                                </dxg:GridControl.View>
                                <dxg:GridColumn FieldName="ID" Width="50" MinWidth="50"/>
                                <dxg:GridColumn FieldName="IsActive" Width="60" MinWidth="60"/>
                                <dxg:GridColumn FieldName="CustomerName" Width="250" MinWidth="250"/>
                            </dxg:GridControl>

                        </dxlc:GroupBox>

                        <dxlc:GroupBox Grid.Row="1" Header="Business Clients">
                            <dxg:GridControl
                                ItemsSource="{Binding Customers}"
                                SelectedItem="{Binding SelectedCustomer}"
                                FilterString="[CustomerType] = 'Business'">
                                <dxg:GridControl.View>
                                    <dxg:TableView AllowPerPixelScrolling="True"/>
                                </dxg:GridControl.View>
                                <dxg:GridColumn FieldName="ID" Width="50" MinWidth="50"/>
                                <dxg:GridColumn FieldName="IsActive" Width="60" MinWidth="60"/>
                                <dxg:GridColumn FieldName="CustomerName" Width="250" MinWidth="250"/>
                            </dxg:GridControl>
                        </dxlc:GroupBox>
                            
                        
                        <StackPanel Grid.Row="2" Orientation="Horizontal">
                            <dx:SimpleButton Content="Active Customers" Glyph="{dx:DXImage Image=Refresh2_16x16.png}" />
                            <dx:SimpleButton Content="ALL Customers" Glyph="{dx:DXImage Image=Refresh_16x16.png}" />
                        </StackPanel>
                    </Grid>
                </dxdo:LayoutPanel>
                
                <dxdo:LayoutPanel AllowHide="False" Caption="Customer Details">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="50"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <Label Content="{Binding SelectedCustomer.CustomerName}" FontSize="36" Padding="20,0"/>

                    </Grid>        
                </dxdo:LayoutPanel>
            
            </dxdo:LayoutGroup>
        </dxdo:DockLayoutManager>
    </Grid>

</UserControl>
